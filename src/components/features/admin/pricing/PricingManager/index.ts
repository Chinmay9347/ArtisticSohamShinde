/**
 * Public export for the PricingManager feature.
 *
 * Keeping the export here allows pages and other features
 * to import PricingManager without knowing the internal
 * component filename.
 */

export { PricingManager } from "./PricingManager";

export type {
  PricingManagerProps,
} from "./PricingManager.types";