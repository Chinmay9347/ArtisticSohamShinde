import { GalleryImporter } from "@/components/admin/gallery/GalleryImporter/GalleryImporter";

export default function GalleryImportPage() {
  return (
    <main className="p-8">
      <GalleryImporter />
    </main>
  );
}